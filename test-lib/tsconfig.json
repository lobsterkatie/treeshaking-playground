{
  "include": ["./src/**/*.js", "./src/**/*.ts"],

  "compilerOptions": {
    // try commenting out the static property in the `Cat` class in `cube.js` and changing this to ES6 - the class will disappear
    "target": "ES5",
    "module": "ES6",
    "outDir": "./dist",
    "allowJs": true
  }
}
